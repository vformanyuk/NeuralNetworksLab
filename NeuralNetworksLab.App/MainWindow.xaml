<Window x:Class="NeuralNetworksLab.App.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:NeuralNetworksLab.App"
        xmlns:controls="clr-namespace:GraphView.Framework.Controls;assembly=GraphView.Framework"
        mc:Ignorable="d"
        Title="MainWindow" Height="680" Width="720">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="100"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="4*"></ColumnDefinition>
            <ColumnDefinition Width="*"></ColumnDefinition>
        </Grid.ColumnDefinitions>

        <Button Content="Add node" Grid.Column="0" Command="{Binding Path=AddNodeCommand, Mode=OneTime}"/>
        <controls:DiagramCanvasControl Grid.Row="1" Grid.Column="0" Diagram="{Binding Path=Diagram, Mode=OneWay}"></controls:DiagramCanvasControl>

        <ListView Grid.Column="1" Grid.Row="0" Grid.RowSpan="2" ItemsSource="{Binding Path=Properties, Mode=OneWay}"></ListView>
    </Grid>
</Window>
