<Window x:Class="NeuralNetworksLab.App.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:NeuralNetworksLab.App"
        xmlns:controls="clr-namespace:GraphView.Framework.Controls;assembly=GraphView.Framework"
        mc:Ignorable="d"
        Title="MainWindow" Height="350" Width="525">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="100"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Button Content="Add node" Command="{Binding Path=AddNodeCommand, Mode=OneTime}"/>
        <controls:DiagramCanvasControl Grid.Row="1" Diagram="{Binding Path=Diagram, Mode=OneWay}"></controls:DiagramCanvasControl>
    </Grid>
</Window>
